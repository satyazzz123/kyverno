apiVersion: kuttl.dev/v1beta1
kind: TestStep
commands:
  - script: |
      if kyverno apply example-policy.yaml --resource resource.yaml -o . 

      then 
        echo "Test succeeded."
        exit 0
      else 
        echo "Test failed"
        exit 1 
      fi
